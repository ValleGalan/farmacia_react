import React from "react";
import { MapView, SearchBar } from '../../components/Admin'

export const HomeMap = () => {
    return (
        <div>
            <MapView />
            <SearchBar />
        </div>
    )
}
